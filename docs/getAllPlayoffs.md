# Feature: Get all playoffs on a dropdown

## Context
On the playoffs page, we need to be able to select the tournament we want to view the bracket for.
The tournament id is on the URL, but we need to be able to change it from a dropdown.
The dropdown should be populated with all the playoffs tournaments.

## Requirements
The endpoint to call is /api/playoffs/getAll